# Copy to .env.local and adjust. Only non-secret values — NEXT_PUBLIC_* is shipped to the browser.

# Backend API base URL (FastAPI dev server)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# true = serve fictional placeholder picks instead of calling the backend (dev/demo only)
NEXT_PUBLIC_USE_MOCK_API=true
